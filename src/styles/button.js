import styled from 'styled-components';

const Button = styled.button`
    border-radius:4px;
    margin-right:0.5rem;
    border:0;
    cursor:pointer;
    padding:0.5rem;
    font-size:0.98rem;
`;

export default Button;